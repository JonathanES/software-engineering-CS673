import React, { Component } from "react";

class Logout extends Component {
  render() {
    return (
      <div className="container-fluid"> back to the sign-in page content</div>
    );
  }
}
export default Logout;
